{
  "name": "rubik-rest-client",
  "version": "1.0.0",
  "private": true,
  "description": "Build process for RubikREST JS Client",
  "scripts": {
    "build": "terser js/RubikREST.js -o js/RubikREST.min.js --compress --mangle --comments false --source-map \"url='RubikREST.min.js.map'\""
  },
  "devDependencies": {
    "terser": "^5.37.0"
  }
}
